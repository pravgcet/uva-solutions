int main() {
    int n;
    while (scanf("%d", &n)) {
        vector<string> names(n);
        map<string, int> balance;
        for (int i = 0; i < n; i++) {
            cin >> names[i];
            balance[names[i]] = 0;
        }
        for (int i = 0; i < n; i++) {
            string name;
            int k, amount;
            cin >> name >> amount >> k;
            string friend;
            int amount
            while (k--) {
                friend
            }
        }
    }
}
